# 設計方針

この教材は、テスト駆動開発の反復をNestJSの実装へ持ち込むことを目的にしています。完成コードを先に設計してテストを後付けするのではなく、次に必要な振る舞いを表す小さなテストから始めます。そのため、フレームワークを起動する範囲を機械的に統一せず、守りたい契約に応じて選びます。

## なぜ読書リストAPIなのか

読書リストには、入力規則、重複判定、保存、一覧取得、HTTPという複数の責務があります。一方で、認証、データベース、非同期ジョブを導入しなくても、NestJSのDIとテストの境界を説明できます。既存のGoタスク管理APIとは題材・言語・フレームワークを分け、タスク管理のCRUD全般ではなく、TDDで境界を増やす順番に焦点を置きます。

| 選択 | この教材で採用したこと | 理由 |
|---|---|---|
| ドメイン表現 | `Book.create()` と明示的な例外 | HTTPに依存しない本質的な入力規則を、起動不要のテストで固定するためです。 |
| 外部依存 | `BookRepository` と `BookIdGenerator` の小さな契約 | サービスがデータベースと乱数に直接依存せず、手書きフェイクで観測できるようにするためです。 |
| DIトークン | `Symbol` を用いた契約トークン | TypeScriptのinterfaceは実行時には存在しないため、NestのDIで実装を差し替える識別子が必要だからです。 |
| 単体テスト | Jestと手書きフェイク | テストの期待値を読みやすくし、モックの呼び出し回数だけに依存しないためです。 |
| コントローラテスト | `Test.createTestingModule()` | Nestのコントローラとプロバイダの配線を最小範囲で検証するためです。 |
| E2Eテスト | `createNestApplication()` とSupertest | ルーティング、JSON変換、HTTPステータスを実際のHTTP境界で確認するためです。 |
| 永続化 | インメモリ実装 | 学習対象をTDDの反復に絞り、外部DBの準備を不要にするためです。 |

## テスト範囲を増やす順番

NestJSの公式ドキュメントは、フレームワークを起動しない孤立テスト、`TestingModule`を利用するテスト、アプリケーション全体を起動するE2Eテストを区別して説明しています。[1] この教材でも同じ方向に、狭いテストから広いテストへ進めます。

```mermaid
flowchart LR
  A[ドメイン規則\n起動しない] --> B[ユースケース\n手書きフェイク]
  B --> C[コントローラ\nTestingModule]
  C --> D[HTTPワークフロー\nNest + Supertest]
```

ここで重要なのは、すべてのテストをE2Eにすることではありません。空白タイトルの扱いが壊れたときはドメインテストが最も速く原因を示します。ルーティングやJSONが壊れたときはE2Eテストが必要です。失敗したときに最も早く意味のある情報を返す範囲を選びます。

## インメモリ実装の制約

`InMemoryBookRepository` はプロセスのメモリだけに本を保存します。アプリケーション再起動後の永続化、複数プロセス間の整合性、重複登録の同時実行制御は対象外です。本番の永続化を追加する場合は、既存のサービスのテストを保ったまま、`BOOK_REPOSITORY` に別実装を結線します。その実装には、DB統合テストを追加してください。

## 参考資料

[1] [NestJS Documentation: Testing](https://docs.nestjs.com/fundamentals/testing)
